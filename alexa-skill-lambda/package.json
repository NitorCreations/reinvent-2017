{
  "name": "alexa-address-skill",
  "version": "0.0.1",
  "description": "Skill that lets Alexa create alerts using her configured device location",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "1.0.0",
    "node-fetch": "1.7.3"
  }
}
